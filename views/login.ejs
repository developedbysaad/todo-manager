<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("head.ejs") %>
    <title><%= title %></title>
  </head>
  <body>
    <div class="grid grid-cols-6 py-4">
      <div class="col-start-3 col-span-2">
        <h1  class="text-2xl font-semibold text-gray-600">My <a href="/" class="text-green-600">Todo List</a></h1>
        <br>
        <h6>Login to your account</h6>
        <span>Don't have an account? <a href="/signup" class="text-blue-600 hover:text-blue-900">Sign Up here </a></span>
        <form action="/session" method="post">
          <% if(messages && messages.error && messages.error.length > 0)  { %>
            <% for(var i=0; i<messages.error.length; i++) { %>
                    <span class="text-red-600 block"> <%= messages.error[i] %></span>
            <% } %>
          <% } %>
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="email">Email:</label>
            <input
              type="email"
              name="email"
              class="border border-gray-300 rounded p-2 text-gray-900 w-full"
            />
          </div>
          <div class="py-3">
            <label for="password">Password:</label>
            <input
              type="password"
              name="password"
              class="border border-gray-300 rounded p-2 text-gray-900 w-full"
            />
          </div>
          <div class="py-3">
            <button
              type="submit"
              class="text-white rounded bg-green-600 hover:bg-green-800 px-5 py-2"
            >
              Sign in
            </button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
